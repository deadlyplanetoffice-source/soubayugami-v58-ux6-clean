# UX24.3L Manual Folder + Hold Tag

- Header: `MDO v58 / UX24.3L`
- Atlas Listの初期分類を自動推定しない。未設定銘柄は必ず「未分類」。
- `保有`をフォルダから除外し、他フォルダと重複できるタグに変更。
- 旧データでfolderが「保有」になっている場合は、表示時に「未分類」+「保有」タグとして扱う。
- 2層目カードに「保有タグ」トグルを追加。
- フォルダ色とタグ色を分離。
