# UX24.3D MDO header + Atlas folder default fix

- App header renamed to `MDO v58 / UX24.3D`.
- Removed the long mobile subtitle `動く銘柄図鑑` from the sticky header.
- Compressed mobile header height and font size.
- Atlas default folder inference adjusted so watched symbols are not automatically sent to `除外・墓場` just because old notes contain words like `除外`.
- `除外・墓場` is now mainly a manual folder, or for non-watched items with explicit exclusion/watch-off wording.
